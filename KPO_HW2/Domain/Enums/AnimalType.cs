namespace KPO_HW2.Domain.Enum
{
    public enum AnimalType
    {
        Predator,    // Хищники (львы, тигры)
        Herbivore,   // Травоядные (слоны, жирафы)
        Omnivore,    // Всеядные (медведи, обезьяны)
        Aquatic,     // Водные (дельфины, пингвины)
        Avian        // Птицы (орлы, попугаи)
    }
}
